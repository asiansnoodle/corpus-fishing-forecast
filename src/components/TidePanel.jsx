// TidePanel.jsx — Lists high/low tide events for the selected day
// Shows timestamps, heights (ft), and rising/falling state

export default function TidePanel({ tideEvents }) {
  return (
    <div>
      {/* TODO: render tide event list with heights and rise/fall indicators */}
      <p className="text-white text-xs">TidePanel placeholder</p>
    </div>
  )
}
